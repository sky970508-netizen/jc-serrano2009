# 저널클럽: 복잡 가중 네트워크의 다중스케일 백본 추출

Serrano MÁ, Boguñá M, Vespignani A (2009)
*Extracting the multiscale backbone of complex weighted networks.*
**PNAS** 106(16):6483–6488. [doi:10.1073/pnas.0808904106](https://doi.org/10.1073/pnas.0808904106)

원문과 한국어 번역을 문단 단위로 교차 배치한 읽기용 페이지입니다.

👉 **<https://sky970508-netizen.github.io/jc-serrano2009/>**

## 보는 법

| | |
|---|---|
| 보기 모드 | `교차` / `2단` / `한글` / `English` / `논문` — 단축키 `1` `2` `3` `4` `P` |
| **논문 모드** | 원문 PNAS 2단 조판 그대로. 한 문장씩 쪼개진 영어를 원래 문단으로 되돌려 보여 준다 |
| **형광펜·메모** | 문장을 드래그 → 노랑(중요)·파랑(질문)·빨강(핵심). `N` 으로 노트 패널을 열면 모아 볼 수 있고, **마크다운으로 복사**하면 그대로 발표 대본이 된다 |
| **공부 가이드** | 절마다 “이 절에서 붙잡을 것 / 여기까지 정리 / 스스로 확인”, 식마다 기호 해설, 그림마다 “여기서 볼 것”, 용어에 마우스를 올리면 정의. `G` 로 끄고 켠다 |
| 이동 | 좌측 목차, `J` / `K` 로 섹션 단위 이동 |
| 그림 | 클릭하면 전체화면, 한 번 더 클릭하면 원본 크기 |
| 기타 | `T` 다크모드, `+` / `−` 글자 크기, `?` 단축키 전체 |

형광펜과 메모는 **브라우저에 저장**된다. 기기를 옮길 때는 노트 패널의 `백업`으로 JSON을 내려받아 다른 기기에서 `복원`하면 된다.

`index.html` 한 파일로 완결되어 있습니다 — 그림과 수식 폰트(KaTeX)가 모두 내장되어 있어 파일을 내려받으면 오프라인에서도 그대로 열립니다.

## 저작권

본문·그림의 저작권은 원저자와 PNAS에 있습니다. 번역과 편집은 저널클럽 내부 스터디 목적입니다.
